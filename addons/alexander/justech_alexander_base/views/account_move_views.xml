<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_dx_draft_cancel" model="ir.ui.view">
        <field name="name">account.move.form.dx.draft.cancel</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="priority">95</field>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="button_cancel" type="object" string="Cancelar" class="btn-secondary" invisible="state != 'draft'" groups="account.group_account_invoice"/>
            </xpath>
        </field>
    </record>
</odoo>
