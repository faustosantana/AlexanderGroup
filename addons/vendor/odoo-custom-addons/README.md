# Vendored snapshot (GENERATED)

**SOURCE OF TRUTH:** https://github.com/faustosantana/odoo-custom-addons

Do **not** edit modules here. Regenerate with:

```bash
# from a machine that can read the canonical private repo:
bash tools/sync_odoo_custom_addons_vendor.sh
```

See `ORIGIN_REF.json` for commit/tag provenance.
